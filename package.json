{
  "name": "gfw",
  "devDependencies": {
    "lerna": "^3.22.1"
  },
  "scripts": {
    "bootstrap": "lerna clean -y && lerna bootstrap",
    "dev:flagship": "lerna run dev --scope @gfw/flagship --stream",
    "build:flagship": "lerna run build --scope @gfw/flagship --stream",
    "build:start": "lerna run start --scope @gfw/flagship --stream",
    "dev:docs": "lerna run start --scope @gfw/docs --stream"
  }
}