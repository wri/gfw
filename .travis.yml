language: ruby
bundler_args: --without development
script: "DISPLAY=:0.0 rspec --format=documentation spec/acceptance"
rvm:
  - 1.9.2
  - 1.9.3
gemfile:
  - Gemfile
#notifications:
  #recipients:
    #- ferdev@vizzuality.com
branches:
  only:
    - master
