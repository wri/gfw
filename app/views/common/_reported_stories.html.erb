<div class="more_stories">
  <div class="inner">
    <h1>Stories reported by users</h1>
    <p>
    <%= link_to('Browse user stories in our map', map_path) %> or <%= link_to('submit yours', new_story_path) %>. People is a key factor in forest clearing detection.
    </p>

    <ul>
      <% @stories.each do |story| %>
        <li>

        <%= link_to image_tag(story_image_or_map(story)), story, :class => :frame %>

        <%= show_exclamation?(story) %>
        </li>
      <% end %>
    </ul>

    <hr />

    <%= link_to (content_tag :span, 'Submit your story'), new_story_path, :class => "btn" %>

  </div>
</div>
