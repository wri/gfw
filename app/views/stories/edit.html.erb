<% content_for :title do %>
  <%= "Edit story" %>
<% end %>

<% content_for :inner_header do %>
  <%= link_to t(".delete"), stories_path(@story.id), method: :delete, data: { confirm: 'Are you sure?' }, :class => "btn green" %>
<% end %>

<%= render 'form' %>
