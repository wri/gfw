import { PureComponent, createElement } from 'react';
import { connect } from 'react-redux';
// import PropTypes from 'prop-types';
// import { withRouter } from 'react-router';

import * as actions from './{{kebabCase name}}-actions';
import reducers, { initialState } from './{{kebabCase name}}-reducers';

import {{properCase name}}Component from './{{kebabCase name}}-component';
// import { {{camelCase name}}Selector } from './{{kebabCase name}}-selectors';

const mapStateToProps = (state) => ({
  state
});

class {{properCase name}}Container extends PureComponent {
  render() {
    return createElement({{properCase name}}Component, {
      ...this.props
    });
  }
}

{{properCase name}}Container.propTypes = {};

export {
  actions,
  reducers,
  initialState
};

export const reduxModule = { actions, reducers, initialState };

export default connect(mapStateToProps, actions)({{properCase name}}Container);
