<label for="select-layers">{{name}}</label>
<div class="m-select">
  <select id="select-region" class="chosen-select default disabled js-select-region" data-placeholder="{{placeholder}}" disabled>
    <option value=""></option>
    {{#each regions}}
      <option value="{{this.id_1}}" {{#if selected}}selected{{/if}}>{{this.name_1}}</option>
    {{/each}}
  </select>
</div>
