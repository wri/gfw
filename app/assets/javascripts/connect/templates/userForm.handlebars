<h2>your profile information</h2>
<p>This information is optional but it will help us learning how you use Global Forest Watch and how to improve it.</p>
<form method="POST" action="{{action}}">
  <label>name</label>
  <input type="text" placeholder="Your name" name="name" value="{{name}}">
  <label>email</label>
  <input type="email" placeholder="Your email" name="email" value="{{email}}">
  <label>organization / profession / job title</label>
  <input type="text" placeholder="Your organization / profession / job title" name="job" value="{{job}}">
  <label>sector</label>
    <input placeholder="Your sector" name="sector" value="{{sector}}">
  <label>primary responsibility</label>
    <input name="responsibility" placeholder="Your primary responsibilities" value="{{responsibilities}}">
  <label>location</label>
    <input name="country" placeholder="Your country" value="{{country}}">
    <input name="state" placeholder="State/department/province" value="{{state}}">
  <label class="m-sel-container">gender
    <select name="gender"><option>Male</option><option>Female</option></select>
  </label>
  <label>how do you use or plan to use global forest watch?</label>
    <input name="use" placeholder="" value="{{use}}">

  <div class="m-improve-gfw">
    <p>Interested in testing new features and helping to improve Global Forest Watch? Sign up to become an official tester!</p>
    <span>
      <label><input type="radio" name="sign_up" value="yes" />Yes, sign me up</label>
      <label><input type="radio" name="sign_up" value="no" />No thanks</label>
    </span>
  </div>
  <input type="text" name="redirect" value="{{redirect}}" class="hidden" >
  <div class="m-buttons">
    <a id="sendform" class="btn green uppercase">continue</a>
    <a id="skipform" class="btn gray uppercase">skip</a>
  </div>
</form>
