<div class="column small-12 medium-4">
  <div class="m-widget-tree-cover-loss-alerts-card">
    <div class="card-content">
      <span class="ranking">{{ranking}}</span>
      <div class="card-data">
        <span class="name">TODO:name</span>
        <div class="alerts">
          <span class="value">{{alerts}}</span>
          <span class="status {{#if isWorst}} -worst{{/if}}"></span>
        </div>
      </div>
    </div>
    <div class="card-chart" id="cover-loss-alert-card-{{ranking}}"></div>
    <div class="card-actions">
      <a class="link" href="#">Hi-Res imagery</a>
      <a class="link" href="#">Subscribe</a>
    </div>
  </div>
</div>
