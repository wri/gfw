<div class="widget">
  <div class="m-widget-annual-tree-cover-loss-ranking">
    <div class="ranking">
      <div class="list">
        <h3 class="heading">Subnational ranking in protected areas:</h3>
        {{#each data}}
          <div class="item">
            <div class="number">{{index}}</div>
            <div class="name">{{name}}</div>
            <div class="value{{#if selected }} -selected{{/if}}">{{value}}<span class="unit">MHa</span></div>
          </div>
        {{/each}}
      </div>
    </div>
  </div>
</div>
