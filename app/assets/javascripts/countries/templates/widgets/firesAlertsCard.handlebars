<div class="small-12 medium-4 widget-fire-alerts-container">
  <div class="m-widget-fires-alerts-card">
    <div class="card-content">
      <div class="ranking">
        <span>{{ranking}}</span>
      </div>
      <div class="card-data">
        <span class="name">{{name}}</span>
        <div class="alerts">
          <span class="value -{{color}} {{#if isWorst}}-worst{{/if}}">{{alerts}}</span>
            <div class="card-actions">
                <a class="link" href="#">Hi-Res imagery</a>
                <a class="link" href="#">Subscribe</a>
            </div>
        </div>
      </div>
    </div>
  </div>
</div>
