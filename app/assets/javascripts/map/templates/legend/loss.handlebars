{{#if threshold}}
  <div class="layer-details">
    <p class="canopy" title="Click the gear icon to change the tree cover canopy density">Displaying {{layerTitle}} with ><span> {{threshold}}</span>% canopy density.</p>
  </div>
{{/if}}
