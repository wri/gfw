{{!-- TO-DO: Let's figure out how to include this elements only for mobile devices --}}
<h3 id="title-dialog-legend" class="title-dialog noselect js-toggle-embed-legend">Legend <span>(Click to expand)</span></h3>
<span class="close js-toggle-legend"><svg><use xlink:href="#shape-close"></use></svg></span>

{{#if this.country}}
  <ul class="categories divided">
    <h2>{{this.country.name}} data</h2>
    
    <div id="legend-country-buttons" class="country-buttons">
      <ul class="m-btncontainer is-space-between">
        <li><a class="btn gray uppercase little full-width download-mobile-link" target="_blank" href="http://data.globalforestwatch.org/datasets?keyword={{this.country.iso}}">Country data</a></li>
        <li><a class="btn gray uppercase little full-width" href="/country/{{this.country.iso}}">Country profile</a></li>
      </ul>
      <div id="legend-country-more"></div>        
    </div>

    {{#each categoriesIso}}
      <li class="category {{#if this.closed}}closed{{/if}}">
        {{#each this}}
          {{#if @first}}
            <span data-category_slug="{{this.category_status}}" class="category-name js-toggle-category" style="color: {{this.category_color}}; border-color: {{this.category_color}};">
              {{this.category_name}}
              <em>({{../../layers_length}})</em>
            </span>
          {{/if}}
        {{/each}}
        <ul class="layers {{#if this.closed}}closed{{/if}}">
          {{#each this}}
            <li class="layer">
              <span class="layer-name">
                <i class="circle" style="color:{{this.title_color}};"></i>
                {{this.title}}
                {{#if this.subtitle}}<span class="subtitle">{{this.subtitle}}</span>{{/if}}
                <a href='#' class='layer-close js-layer-close' data-slug="{{this.slug}}"><span>Delete</span> <svg><use xlink:href="#shape-close"></use></svg></a>
                {{#if this.source_json}}
                  <a href='#' data-source='{{this.source_json}}' class='source'><svg><use xlink:href="#shape-info"></use></svg></a>
                {{else}}
                  {{#if this.source}}
                    <a href='#' data-source='{{this.source}}' class='source'><svg><use xlink:href="#shape-info"></use></svg></a>
                  {{/if}}
                {{/if}}
              </span>
              {{#if this.detailsTpl}}
                {{{this.detailsTpl}}}
              {{/if}}
              {{#if this.sublayer}}
                <div class="layer-sublayer js-toggle-sublayer" data-sublayer="{{this.sublayer}}">
                  <span class="onoffswitch {{this.geographic}} {{#if this.geographic}}style="background-color:#f69;"{{/if}}"><span></span></span>
                  <div class="sublayer-title">Geographic coverage</div>
                </div>
              {{/if}}
            </li> {{!-- /layer --}}
          {{/each}}
        </ul>  {{!-- /layers --}}
      </li> {{!-- /category --}}
    {{/each}}
  </ul> {{!-- /categories --}}
{{/if}}
{{#if categories}}
<ul class="categories {{#if country}}divided{{/if}}">
  {{#if country}}
    <h2>Global data</h2>
  {{/if}}
  {{#each categories}}
    <li class="category {{#if this.closed}}closed{{/if}}">
      {{#each this}}
        {{#if @first}}
          <span data-category_slug="{{this.category_status}}" class="category-name js-toggle-category" style="color: {{this.category_color}}; border-color: {{this.category_color}};">
            {{this.category_name}}
            <em>({{../../layers_length}})</em>
          </span>
        {{/if}}
      {{/each}}
      <ul class="layers {{#if this.closed}}closed{{/if}}">
        {{#each this}}
          <li class="layer">
            <span class="layer-name">
              <i class="circle" style="color:{{this.title_color}};"></i>
                {{this.title}}
                {{#if this.subtitle}}<span class="subtitle">{{this.subtitle}}</span>{{/if}}
              <a href='#' class='layer-close js-layer-close' data-slug="{{this.slug}}"><span>Delete</span> <svg><use xlink:href="#shape-close"></use></svg></a>
              {{#if this.source_json}}
                <a href='#' data-source='{{this.source_json}}' class='source'><svg><use xlink:href="#shape-info"></use></svg></a>
              {{else}}
                {{#if this.source}}
                  <a href='#' data-source='{{this.source}}' class='source'><svg><use xlink:href="#shape-info"></use></svg></a>
                {{/if}}
              {{/if}}
            </span>
            {{#if this.detailsTpl}}
              {{{this.detailsTpl}}}
            {{/if}}
            {{#if this.sublayer}}
              <div class="layer-sublayer js-toggle-sublayer" data-sublayer="{{this.sublayer}}">
                <span class="onoffswitch {{this.geographic}}" {{#if this.geographic}}style="background-color:#f69;"{{/if}}><span></span></span>
                <div class="sublayer-title">Geographic coverage<a href='#' data-source='{{this.source}}_source' class='source'><svg><use xlink:href="#shape-info"></use></svg></a></div>

              </div>
            {{/if}}
          </li> {{!-- /layer --}}
        {{/each}}
      </ul>  {{!-- /layers --}}
    </li> {{!-- /category --}}
  {{/each}}
</ul> {{!-- /categories --}}
{{/if}}
<div id="button-box-embed-legend" class="button-box-embed-legend">
  <a id="link-embed-legend" target="_blank" href="#" class="btn green uppercase full-width">View on GFW</a>
</div>
